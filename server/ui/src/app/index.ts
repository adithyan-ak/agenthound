// app layer barrel — composition root (entry, routes, providers, layout).
// May import features, entities, and shared. See ARCHITECTURE.md.
export { AppRoutes } from "./routes";
export { AppProviders } from "./providers";
